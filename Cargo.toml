[package]
name = "gra"
version = "0.1.0"
edition = "2024"

[profile.dev]
debug = false

[dependencies]
bevy = { path = "../bevy" , default-features = false, features = ["2d", "ui", "dynamic_linking"] }
